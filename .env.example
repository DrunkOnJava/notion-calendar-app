# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000
# NODE_ENV is automatically set by Next.js (development/production/test)
# Do not manually override it to avoid build issues

# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Server-side only (NEVER expose these in client code)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
SUPABASE_JWT_SECRET=your_supabase_jwt_secret

# Database Connection Strings (for Prisma/direct SQL)
# Get these from: https://supabase.com/dashboard/project/_/settings/database
POSTGRES_URL=your_postgres_connection_string
POSTGRES_PRISMA_URL=your_postgres_prisma_connection_string
POSTGRES_URL_NON_POOLING=your_postgres_non_pooling_connection_string

# Individual Database Connection Parameters
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_HOST=your_postgres_host
POSTGRES_DATABASE=postgres

# Notion Integration (if applicable)
# NOTION_API_KEY=your_notion_api_key
# NOTION_DATABASE_ID=your_database_id

# Analytics Configuration
# Vercel Analytics and Speed Insights are automatically enabled in production
# These variables control debug and development behavior

# Enable analytics debug mode (shows events in console)
# Default: false in production, true in development
NEXT_PUBLIC_DEBUG_ANALYTICS=false

# Force enable analytics in non-production environments (for testing)
# Default: false (analytics only run in production)
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# Note: Analytics are automatically enabled when deployed to Vercel
# No API keys needed - Vercel handles authentication automatically
